# Core Django & Web
Django
django-cors-headers
gunicorn
whitenoise

# Firebase
firebase-admin==6.3.0

# TensorFlow & Deep Learning
tensorflow
tensorflow-hub

# Audio Processing
librosa
soundfile
audioread

# Scientific Computing (required by librosa)
scipy

# Utilities
requests
python-dotenv

# Note: Removed unnecessary packages that were only used in training/CLI:
# - pandas (only used in CSV export, not in web API)
# - tensorboard (only for training visualization)
# - pillow (not used anywhere)
# - tqdm (progress bars for CLI)
# - scikit-learn (not used in production)
# - keras (included in tensorflow)
# - h5py, protobuf, grpcio (auto-installed by tensorflow/firebase)
# - asgiref, wrapt, six, typing_extensions (auto-installed as dependencies)
